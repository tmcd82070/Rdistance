% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drawTransects.R
\name{drawTransects}
\alias{drawTransects}
\title{Draw random transects given target length}
\usage{
drawTransects(
  sPoly,
  targetLength,
  type = c("rectangular", "zigzag"),
  angle = units::set_units(0, "degrees"),
  spacing = NULL,
  baseline = NULL,
  combine = TRUE,
  target = c("total", "onEffort"),
  R = 1,
  minLength = units::set_units(100, "m"),
  tolerance = 0.01,
  nfmax = 50,
  minSolidity = 0.75,
  plot = FALSE
)
}
\arguments{
\item{sPoly}{An \code{sf} or \code{sfc} object containing the polygon(s)
(\code{MULTIPOLYGON} or \code{POLYGON} geometries) within which
transects will be placed. \code{sPoly} must be projected to a planar
coordinate system whose linear unit is meters (ideally an equal-area
projection so areas are undistorted). Geographic coordinates are rejected.}

\item{targetLength}{The desired transect length across \code{sPoly}, interpreted
according to \code{target} (total length by default). Must be a \code{units} length
object, e.g. \code{units::set_units(1000, "km")}. Effort is allocated to polygons
in proportion to area.}

\item{type}{The transect layout, one of:
\itemize{
\item \code{"rectangular"} (the default): parallel, equally spaced straight lines.
\item \code{"zigzag"}: a single continuous path that zig-zags between opposite edges
of the polygon.
}}

\item{angle}{Orientation of \code{"rectangular"} transects.  Must have
units convertible to 'degrees' (i.e., either 'degrees' or 'radians').
\verb{angle = 0 \[°\]} (the default) produces North-South transects; \verb{angle = 90 \[°\]}
produces East-West transects. Ignored when \code{type = "zigzag"}.}

\item{spacing}{Optional transect spacing, as a \code{units} length object. When
supplied, computing the spacing to achieve \code{targetLength} in the study
area is skipped and this spacing is used directly.
When \code{NULL} (the default) the spacing to achieve approximately \code{targetLength}
transects (including connectors) is computed using \code{\link[=findSpacing]{findSpacing()}}.}

\item{baseline}{Optional \code{sf}/\code{sfc} \code{LINESTRING} giving the reference line
for \code{"zigzag"} transects. Relevant only to \code{"zigzag"} transect.
To construct zig-zag transects, the routine places points along
the baseline spaced \code{spacing} apart (with a random start), then draws
segments perpendicular to this baseline that span the polygon.
The routine then connects
every other point on the polygon boundary where the perpendicular segment
intersects the boundary and this forms the zig-zag transect.
Points along the boundary where \code{"zigzag"}
transect legs meet are called "pivots".
When \code{NULL} (the default) a straight baseline is estimated from
the polygon (see Details). For \code{"rectangular"} transects the baseline does
not influence construction (see \code{angle}). Supplying a
baseline is only allowed when \code{sPoly} contains a single polygon. User supplied
baselines are extended past the polygon's bounding box before use, so that
pivots can be placed across the entire polygon.}

\item{combine}{Logical controlling the geometry(ies) in the returned object. Both
forms have the same \code{onEffortLength} and \code{totalLength} totals but differ
in the geometry returned.
\itemize{
\item \code{TRUE} (the default): return the route actually flown, one continuous
\code{LINESTRING} per polygon.  Parallel legs for \code{"rectangular"} transects
are joined by transit legs to form a back-and-forth type route.
\code{"zigzag"} transects do not have transit legs between on-effort legs.
The route is \emph{not} clipped to the polygon,
so off-effort transit legs can run outside the polygon.
Sum of the returned lengths is \code{totalLength}.
\item \code{FALSE}: return one row per transect leg, with no transit legs.
Sum of the returned lengths is \code{onEffortLength} in the returned object.
Transit legs are clipped to the polygon so a leg that crosses a concavity
will be broken into several pieces and is
returned as a single \code{MULTILINESTRING}, so the returned geometries can be
a mix of \code{MULTILINGSTRING} and \code{LINESTRING}.
When \code{combine == FALSE} and \code{type == "rectangular"}, the \code{totalLength}
attribute \emph{includes} the off-effort transit leg to the next on-effort leg,
even though the transit leg is not returned in the geometry.
}}

\item{target}{Character string selecting what \code{targetLength} measures:
\code{"total"} (the default) is the total transect length including off-effort
transit; \code{"onEffort"} is the on-effort survey length only. The two differ by
the connectors between parallel legs for \code{"rectangular"} transects, and for
\code{"zigzag"} transects only by whatever part of the continuous route a
concavity pushes outside the polygon (nothing, when the polygon is convex).
Use \code{"onEffort"} when \code{targetLength} comes from a survey-effort calculation
such as \code{\link[=calcLineLength]{calcLineLength()}}.}

\item{R}{Number of independent random replicates to generate, passed to
\code{\link[=makeLines]{makeLines()}}. Defaults to 1. When \code{R > 1} the return contains \code{R}
realized transects, each with its own random start.  Replicates are
identified by the \code{id} column.}

\item{minLength}{Minimum length for an individual transect leg to be
counted, as a \code{units} length object. Legs shorter than \code{minLength} are
dropped when measuring the realized length. Defaults to
\code{units::set_units(100, "m")}.}

\item{tolerance}{Relative tolerance on the total length, passed to
\code{\link[OSCARS:oscars]{OSCARS::oscars()}} as its objective tolerance \code{fTol}. Defaults to 0.01.}

\item{nfmax}{Maximum number of objective-function evaluations for
\code{\link[OSCARS:oscars]{OSCARS::oscars()}}. Defaults to 50.}

\item{minSolidity}{For \code{"zigzag"} transects with an estimated baseline, a
warning is issued when a polygon's solidity (area divided by convex-hull
area) falls below this value, because a single zigzag baseline may not cover
a strongly concave polygon. The warning recommends splitting the polygon
into more-convex pieces with \code{\link[=convexPartition]{convexPartition()}} and re-running
\code{drawTransects()} on the pieces. Set to 0 to disable. Defaults to 0.75.}

\item{plot}{Logical. If \code{TRUE}, the polygon(s) and baseline(s) are plotted
on the current graphics device to show their relative orientation.
Baselines are dashed blue lines. Spacing in call to \code{\link[=makeLines]{makeLines()}} is
distance along this line between transects. Rectangular type transects
are perpendicular to this baseline.  Defaults to \code{FALSE}.}
}
\value{
An \code{sf} data frame of transect lines (geometries are
either \code{LINESTRING} or \code{MULTILINESTRING}; a leg that clipping broke into
pieces is one \code{MULTILINESTRING} row) with columns:
\item{transectType}{The layout used, \code{"rectangular"} or \code{"zigzag"}.}
\item{id}{Replicate label, e.g. \code{"Replicate0001"}.}
\item{polygon}{Integer index of the source polygon. Source polygons and
indices are
given in attribute "summary". }
\item{leg}{Integer identifier of the leg within its polygon (1 when
\code{combine = TRUE}, in which case the route is one long line with connectors).}
\item{spacing}{The actual spacing used.}
\item{onEffortLength}{On-effort (surveyed) length of the transect(s)
represented on the row, i.e., the part inside the polygon. With units.
Equals the length of the row's geometry when \code{combine = FALSE}.}
\item{totalLength}{Total transect length of the segment(s) represented on
the row. Total transect length includes the on-effort segment as well as
the off-effort segment connecting to the next on-effort leg. With units.}

A summary of the design is attached as
an attribute \code{attr(x, "summary")}, a list with the layout
\code{type}, the number of polygons \code{nPolygons}, the number of replicates
\code{nReplicates},  \code{targetLength} (\code{NA} unless set by
\code{\link[=drawTransects]{drawTransects()}}), and \code{polygons}.  The \code{polygons} element is an
\code{sf} \code{LINESTRING} data frame whose
geometry column, \code{baseline}, holds each polygon's baseline, with columns
\code{polygon} (integer index), \code{area} (with units), and \code{solidity} (measure of
concavity; 1 = concave; <1 = less concave).
}
\description{
Designs and draws random line transects inside one or more polygons. Can
either be given a target \emph{total} transect length or fixed spacing
determined elsewhere.
This is a convenience wrapper for
\code{\link[=findSpacing]{findSpacing()}}, which computes spacing that yields the targeted
length, then \code{\link[=makeLines]{makeLines()}}, which actually places transects with a
given spacing using a random start. Typically
used after target line length has been estimated using \code{\link[=calcLineLength]{calcLineLength()}}
or another method.
}
\details{
\code{targetLength} is the desired transect length, interpreted as either total
length (including off-effort transit between legs) or on-effort length,
according to \code{target}. The realized length will be as close to this
value as the geometry and the (integer) number of transects allow. This
amount of effort is
allocated across polygons in proportion to area.
}
\examples{
# A simple tapered survey polygon (Alaska Albers, meters).
poly <- sf::st_sf(geometry = sf::st_sfc(sf::st_polygon(list(rbind(
  c(0, 0), c(60000, 0), c(60000, 25000), c(0, 10000), c(0, 0)))),
  crs = 3338))

# Five replicate zigzag designs for a 150 km survey. Totals vary by
# replicate, so summarize the output columns grouped by the replicate id.
set.seed(439752)
zz  <- drawTransects(poly, units::set_units(150, "km"), type = "zigzag", R = 5)
tgt <- attr(zz, "summary")$targetLength
zz |> 
  sf::st_drop_geometry() |>
  dplyr::group_by(id) |>
  dplyr::summarize(onEffortLength = sum(onEffortLength),
                   totalLength    = sum(totalLength),
                   pctOfTarget    = totalLength / tgt)

# Realistic polygons (exceeds CRAN time limits).
\donttest{
data(exampleSurveyPoly)

# Workflow 1: Sample size driven
trgLen = calcLineLength(sPoly = exampleSurveyPoly[1,]
               , N = 1200
               , p = 0.5
               , w = units::set_units(200, "m")
               , targetGroups = 300
               , avgGroupSize = 2.5
               )
transects <- drawTransects(sPoly = exampleSurveyPoly[1,]
                         , targetLength = trgLen
                         , target = "onEffort"
                         )
                         
# Workflow 2: Budget driven
costPer = units::set_units(1000, "1/hr") # dollars/hr for aircraft
budget = 4000 # dollars in budget
speed = units::set_units(85, "miles/hr")
transects <- drawTransects(sPoly = exampleSurveyPoly$geometry[1]
                         , targetLength = speed * budget / costPer
                         , target = "total"
                         )

                          
many <- drawTransects(exampleSurveyPoly, units::set_units(300, "km"),
                      type = "zigzag")
}

}
\seealso{
\code{\link[=findSpacing]{findSpacing()}} and \code{\link[=makeLines]{makeLines()}} for the two individual steps;
\code{\link[=calcLineLength]{calcLineLength()}} for estimating \code{targetLength}.
}
\author{
Trent McDonald.
}
