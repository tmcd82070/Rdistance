% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/findSpacing.R
\name{findSpacing}
\alias{findSpacing}
\title{findSpacing - Transect spacing for a target survey length}
\usage{
findSpacing(
  sPoly,
  targetLength,
  type = c("rectangular", "zigzag"),
  angle = 0,
  baseline = NULL,
  combine = TRUE,
  target = c("total", "onEffort"),
  minLength = units::set_units(100, "m"),
  tolerance = 0.01,
  nfmax = 50,
  minSolidity = 0.75,
  plot = FALSE
)
}
\arguments{
\item{sPoly}{An \code{sf} or \code{sfc} object containing the polygon(s)
(\code{MULTIPOLYGON} or \code{POLYGON} geometries) within which
transects will be placed. \code{sPoly} must be projected to a planar
coordinate system whose linear unit is meters (ideally an equal-area
projection so areas are undistorted). Geographic coordinates are rejected.}

\item{targetLength}{The desired transect length across \code{sPoly}, interpreted
according to \code{target} (total length by default). Must be a \code{units} length
object, e.g. \code{units::set_units(1000, "km")}. Effort is allocated to polygons
in proportion to area.}

\item{type}{The transect layout, one of:
\itemize{
\item \code{"rectangular"} (the default): parallel, equally spaced straight lines.
\item \code{"zigzag"}: a path that zig-zags between opposite edges of the polygon
along a baseline.
}}

\item{angle}{Orientation of \code{"rectangular"} transects, in degrees.
\code{angle = 0} (the default) produces North-South transects; \code{angle = 90}
produces East-West transects. Ignored when \code{type = "zigzag"}.}

\item{baseline}{Optional \code{sf}/\code{sfc} \code{LINESTRING} giving the reference line
for the transects. For \code{"zigzag"} transects the baseline is the line along
which pivots are spaced; when \code{NULL} (the default) a curved centerline is
estimated from the polygon. For \code{"rectangular"} transects the baseline is
only used for reporting and plotting; when \code{NULL} it is a straight line
through the polygon centroid perpendicular to the transects. Supplying a
baseline is only allowed when \code{sPoly} contains a single polygon.}

\item{combine}{Logical controlling the shape of the returned object.
\itemize{
\item \code{TRUE} (the default): return one continuous \code{LINESTRING} per polygon
(parallel legs joined into a mow-the-grass route; zigzags are already
continuous).
\item \code{FALSE}: return one row per transect leg; for rectangular transects each
leg's \code{totalLength} then includes the off-effort distance to the next leg.
}}

\item{target}{Character string selecting what \code{targetLength} measures:
\code{"total"} (the default) is the total transect length including off-effort
transit between legs; \code{"onEffort"} is the on-effort survey length only. The
two differ only for \code{"rectangular"} transects (zigzags have no off-effort
transit, so total equals on-effort). Use \code{"onEffort"} when \code{targetLength}
comes from a survey-effort calculation such as \code{\link[=calcLineLength]{calcLineLength()}}.}

\item{minLength}{Minimum length for an individual transect leg to be
counted, as a \code{units} length object. Legs shorter than \code{minLength} are
dropped when measuring the realized length. Defaults to
\code{units::set_units(100, "m")}.}

\item{tolerance}{Relative tolerance on the total length, passed to
\code{\link[OSCARS:oscars]{OSCARS::oscars()}} as its objective tolerance \code{fTol}. Defaults to 0.01.}

\item{nfmax}{Maximum number of objective-function evaluations for
\code{\link[OSCARS:oscars]{OSCARS::oscars()}}. Defaults to 50.}

\item{minSolidity}{For \code{"zigzag"} transects with an estimated baseline, a
warning is issued when a polygon's solidity (area divided by convex-hull
area) falls below this value; the warning recommends splitting the polygon
with \code{\link[=convexPartition]{convexPartition()}} and re-running on the pieces. Set to 0 to disable.
Defaults to 0.75.}

\item{plot}{Logical. If \code{TRUE}, the polygons, baselines, and generated
transects are drawn on the current graphics device. When \code{R > 1}, each
replicate is drawn in a different colour (\code{rainbow(min(R, 50))}, cycling if
\code{R > 50}). Defaults to \code{FALSE}.}
}
\value{
A \code{units} length object: the approximate spacing that yields
\code{targetLength}. Pass it to \code{\link[=makeLines]{makeLines()}} to place transects.
}
\description{
Compute the approximate transect spacing that yields a target \emph{total}
transect length inside one or more polygons. The spacing is found by
minimizing the difference between the realized total length and
\code{targetLength} with \code{\link[OSCARS:oscars]{OSCARS::oscars()}}. A single spacing is returned and,
applied uniformly, distributes effort across polygons in proportion to
their area. \code{\link[=findSpacing]{findSpacing()}} performs the calculation; \code{\link[=makeLines]{makeLines()}} does
the random placement; \code{\link[=drawTransects]{drawTransects()}} chains the two.
}
\details{
\code{spacing} is interpreted as the perpendicular distance between parallel
lines for \code{"rectangular"} transects, and as the pivot-to-pivot distance
along the baseline for \code{"zigzag"} transects. Because a smaller spacing packs
more transect into each polygon, the total length is a decreasing function
of spacing, which \code{\link[OSCARS:oscars]{OSCARS::oscars()}} searches over a single scalar.
}
\examples{
poly <- sf::st_sf(geometry = sf::st_sfc(sf::st_polygon(list(rbind(
  c(0, 0), c(60000, 0), c(60000, 25000), c(0, 10000), c(0, 0)))),
  crs = 3338))
findSpacing(poly, units::set_units(150, "km"), type = "zigzag")

}
\seealso{
\code{\link[=makeLines]{makeLines()}}, \code{\link[=drawTransects]{drawTransects()}}, \code{\link[OSCARS:oscars]{OSCARS::oscars()}}.
}
